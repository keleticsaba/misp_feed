# MISP Feed Configuration
# Deploy to /etc/misp-feed.conf with: sudo install -m 640 -o root -g misp misp-feed.conf /etc/misp-feed.conf

MISP_URL="https://misp14.berylia.org/attributes/restSearch"
MISP_TOKEN="token"
FEED_DIR="/home/misp/feed"
